# 更新日志

- 修改`.gitignore`，过滤Pycharm一系列文件
- 再次更新`.gitignore`，使用Github官方ignore
- 增加`pyproject.toml`，使用先进的PDM包管理工具
- fix: 错误已知错误
  - 将 `PyKeyboard` 改为 `PyKeyboard`
  - 修改软件图标路径
  - 将旧版本 `get_duration(filename= )` 改为 `path= ``